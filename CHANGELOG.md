# 更新日志

本项目的所有重要变更都记在这里。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.0.0] — 2026-09-14

首个公开发布版本。

### 核心能力

- **自动识别劣质节点**：`bigota` / `hugeota` 会返回看似正常的 `206` 响应，
  实测却被限速到约 16 KB/s。普通下载工具会被骗进去然后卡死，mirom 会自动跳过。
- **高并发分片 + 工作窃取**：所有连接共享一个分片队列，快的连接自然多干活。
- **并发爬坡找拐点**：自动测出最佳连接数，并按"既快又稳"挑线路（吞吐差 15% 内时优先选抖动小的）。
- **尾部防塌陷**：检测到并行度塌陷时，让在途连接交出最大的若干分片重新排队，并补入备用线路。
- **断点续传**：随时中断，重开自动接着下。1 MB 粒度位图。
- **MD5 完整性校验**：从**文件名内嵌的哈希前缀**提取期望值比对。
- **按盘型自适应**：自动区分固态 / 机械 / 网络盘 / 可移动介质，分别决定并发上限。

### 图形界面

- PySide6 + PyQt-Fluent-Widgets 深色界面，实时速度折线图（pyqtgraph）
- 5 个节点的实时状态徽章、4 阶段指示器、关键事件标注
- **设置对话框**（`Ctrl+,`）：并发数 / 限速 / 代理 / 多线路聚合 / MD5 校验 / 预分配 等
- **同名文件冲突弹窗**：下载副本 / 覆盖重下 / 跳过 / 取消
- **工具菜单**：引擎自检（带结果弹窗）、导出测试数据 CSV、诊断报告
- **帮助菜单**：本地使用说明、常见问题、实现思路、检查更新、反馈问题
- 桌面快捷方式、开机自启（可开关）
- 多分辨率图标（16~256），EXE 属性页含作者信息

### 实测成绩（千兆网络）

| 机型 | 大小 | 耗时 | 平均速度 |
|---|---|---|---|
| marble | 6.898 GB | 1:39 | 71 MB/s |
| renoir | 5.795 GB | 1:30 | 65 MB/s |
| pipa | 4.713 GB | 1:39 | 48 MB/s |
| davinci | 2.380 GB | 0:42 | 57 MB/s |
| lime | 3.637 GB | 0:51 | 72 MB/s |

全部通过文件名内嵌 MD5 校验。

### 交叉平台

- Windows / Linux / macOS（引擎部分）
- 纯标准库实现，无 `requests`、无 aria2c、无编译产物
- 兼容 Python 3.6+

---

## 开发过程中修掉的关键问题

这一节记录**踩过的坑**，因为它们都很隐蔽，且大多只在特定条件下才暴露。

### 进度条虚高最多 27%

进度原本用"写了多少字节"（`written`）计算，而完整性位图只在**整块分片下完**时才标记。
两者在传输途中必然有差，实测最大差 659.5 MB（占 2.38 GB 文件的 27%），
而且 `written` 会**超过文件大小** → 被 `min()` 一夹，进度条在文件还差 22 MB 时就显示
`100.00%` 然后长时间不动。

**修法**：位图改为按 1 MB 单元增量标记。差值降到 42 MB（1.7%）——正好是
"每条在途连接手里那半个单元"，理论下限。

### 点「取消」在探测阶段完全无效

探测节点 + 单连接基准测速合计 40 秒以上，而它们都发生在 `Downloader` 创建**之前**。
取消只认 `Downloader.stop`，于是这 40 秒里点取消毫无作用 —— 界面还在打印
"已请求取消"骗用户。

**修法**：加线程级取消标志，在阶段边界、`autotune` 内部、`stall_check` 内部都能查到。
响应时间从"无响应"降到 0.0~2.3 秒。

### 续传判据写错导致续传等于白做

取消后的数据文件是**预分配**过的，大小和完整文件一模一样。所以"文件存在且大小相符"
区分不出"下好了"和"下了一半"，只有**断点文件**能区分。

写错的后果：每次续传都要把整个文件重算一遍 MD5（2.4 GB 要 4 秒），算完必然"不通过"，
再拿这个假结论去重下。

### 挂死检测的 deadline 形同虚设

`read(256KB)` 会一直阻塞到攒够 262144 字节。而劣质节点是**涓流**不是死连接 ——
数据一直在来，socket 超时永远不触发 —— 于是一次 read 就吃掉 12 秒，把 8 秒的预算顶穿。

**修法**：32 KB 小块读 + 每次按剩余预算设超时。单节点检测从 12.15 秒降到 8.29 秒。

### `sys.stdout is None` 导致 EXE 一启动就崩

PyInstaller `--windowed` 出来的 EXE 没有控制台。CPython 的 `print()` 在
`sys.stdout is None` 时是静默 no-op，但显式调 `.isatty()` / `.write()` 会直接
`AttributeError`。

**修法**：加 `_stdout_ok()` 判空，所有输出走 `_safe_write`。

### 冻结的 EXE 会弹出 PowerShell 黑框

探测磁盘类型要调 `Get-PhysicalDisk`。冻结的 `--windowed` 进程没有控制台，
Windows 就给子进程**新分配一个**控制台窗口 —— 就是那个一闪而过的黑框。

**修法**：`CREATE_NO_WINDOW` + `SW_HIDE` 双保险，并且同一个盘符只问一次（缓存）。

### 同名文件冲突被默默跳过

引擎发现同名文件 + MD5 通过就直接返回成功，用户以为工具坏了。
**修法**：弹窗给四个明确选项（下载副本 / 覆盖重下 / 跳过 / 取消）。

### 带查询串的链接会生成非法文件名

`...zip?token=ABC` 里的 `?` 在 Windows 是非法字符，`open()` 会在写盘那一刻才炸；
而且内嵌 MD5 也提取不出来（正则要求 `.zip` 结尾）→ **静默跳过校验**。
**修法**：请求路径保留查询串，本地文件名剥掉。

### 帮助菜单指向占位地址

`REPO_URL` 一直是 `https://github.com/`，点「使用说明」只会打开 GitHub 首页；
`_open_url` 里 `except: pass` 把所有失败都吞掉，用户点完什么都没发生。

**修法**：指向真实仓库与真实锚点；打不开浏览器时把链接摆出来并复制到剪贴板，
而不是静默失败。

---

## 已知限制

- **暂停粒度约 4~5%**：暂停时正在传的分片要写完才停。这是为了保证续传不出洞，
  属于设计取舍而非缺陷。
- **Windows 路径上限 260 字符**：超长路径会提前拦下并提示换浅目录。
- **不检测真实 206 之外的挂死形态**：目前只覆盖"返回正常头但涓流"这一类。
- **虚拟桌面搬运依赖前台权限**：用户正在别的程序里时 Windows 会拒绝
  `SetForegroundWindow`，此时搬运无效属于系统行为。
