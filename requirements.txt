# mirom 运行依赖
#
# 只有图形界面需要这些。引擎本身 (mirom.py / vdesk.py) 是【纯标准库】的,
# 不装任何东西也能跑命令行版本:
#     python mirom.py <链接> --probe
#
# 版本号用 >= 而不是 ==: 这几个库的 API 很稳定, 钉死版本反而会让
# 新系统装不上。实测可用的版本写在注释里。

PySide6>=6.6            # 实测 6.11.2
PySide6-Fluent-Widgets>=1.7   # 实测 1.11.3  (UI 组件库)
pyqtgraph>=0.13         # 实测 0.14.0   (速度折线图)
numpy>=1.24             # 实测 2.5.3    (pyqtgraph 的依赖)
